services:
  mysql-test:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: strayspotter_database_test
    ports:
      - "3307:3306"
    tmpfs:
      - /var/lib/mysql
